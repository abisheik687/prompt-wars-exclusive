{
  "name": "clausewise",
  "version": "1.0.0",
  "private": true,
  "description": "Grounded legal document understanding prototype",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check src/legal-core.js && node --check src/document-reader.js && node --check src/gemini-client.js && node --check functions/index.js"
  }
}
