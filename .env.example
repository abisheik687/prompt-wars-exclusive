# Set this with `firebase functions:secrets:set GEMINI_API_KEY`; never commit the real key.
GEMINI_API_KEY=
# Optional extra production origin. Firebase Hosting origins are allowed automatically.
ALLOWED_ORIGIN=
