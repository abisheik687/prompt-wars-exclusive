# Set this with `firebase functions:secrets:set GEMINI_API_KEY`; never commit the real key.
GEMINI_API_KEY=
# The production Firebase Hosting origin permitted to call the function.
ALLOWED_ORIGIN=https://YOUR_PROJECT.web.app
